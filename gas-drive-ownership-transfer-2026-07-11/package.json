{
  "name": "gas-drive-ownership-transfer",
  "version": "0.1.0",
  "private": true,
  "description": "Google Drive 内で自分が所有するファイル/フォルダの所有権を、指定ユーザーへ一括譲渡する Google Apps Script",
  "scripts": {
    "check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp src/appsscript.json dist/appsscript.json"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.11",
    "typescript": "^5.9.0"
  }
}
