# html-text-extractor

Vite + React で作る、HTML からテキストのみを抽出するツール。GitHub Pages にデプロイされる。

公開先: https://pollenjp-org.github.io/sandbox/html-text-extractor-2026-05-04/

## 開発

```sh
npm ci
npm run dev
```

## ビルド

```sh
npm run build
```

GitHub Pages 配下にデプロイする際は `VITE_BASE` を設定する (CI 側で実施)。
